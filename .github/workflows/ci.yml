name: CI

on:
  push:
    branches: [develop, main, master]
  pull_request:
    branches: [develop, main, master]

jobs:
  # 防止直接推送到保护分支
  prevent-direct-push:
    name: Prevent Direct Push
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && (github.ref == 'refs/heads/develop' || github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
    steps:
      - name: Block direct push to protected branch
        run: |
          echo "❌ 直接推送到保护分支被禁止!"
          echo "请使用Pull Request进行代码合并"
          echo "推送的分支: ${{ github.ref }}"
          echo "触发事件: ${{ github.event_name }}"
          exit 1

  swiftlint:
    name: SwiftLint
    runs-on: macos-15

    steps:
      - uses: actions/checkout@v4

      - name: Install SwiftLint
        run: |
          echo "Installing SwiftLint via Homebrew..."
          if ! command -v brew &> /dev/null; then
            echo "Homebrew not found, installing..."
            /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
          fi
          brew install swiftlint

      - name: Run SwiftLint
        run: |
          swiftlint --reporter github-actions-logging --strict

  build:
    name: Build
    runs-on: macos-15

    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode version
        run: sudo xcode-select -s /Applications/Xcode_16.2.app/Contents/Developer

      - name: Show Xcode version
        run: xcodebuild -version

      - name: Install Fastlane via Ruby Gem
        run: |
          echo "Installing Fastlane via Ruby gem..."
          sudo gem install fastlane -v 2.228.0
          echo "Installed fastlane version:"
          fastlane --version

      - name: Verify CI simulator availability
        run: |
          echo "=== Xcode 版本信息 ==="
          xcodebuild -version
          echo ""

          echo "=== 可用的 iOS 运行时 ==="
          xcrun simctl list runtimes available | grep iOS
          echo ""

          echo "=== 可用的 iPhone 16 设备 ==="
          xcrun simctl list devices available | grep "iPhone 16" | head -5
          echo ""

          echo "=== 自动选择最佳版本 ==="
          # 获取最新可用的 iOS 版本
          AVAILABLE_IOS=$(xcrun simctl list runtimes available | grep iOS | tail -1 | sed 's/.*iOS \([0-9]\+\.[0-9]\+\).*/\1/')
          echo "检测到最新可用版本: iOS $AVAILABLE_IOS"
          echo "IOS_VERSION=$AVAILABLE_IOS" >> $GITHUB_ENV

      - name: Build with Fastlane
        run: |
          echo "Building with iOS $IOS_VERSION + iPhone 16..."
          echo "Using dynamically detected iOS version: $IOS_VERSION"
          fastlane build

  # test:
  #   name: Test
  #   runs-on: macos-15
  #   needs: build
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Select Xcode version
  #       run: sudo xcode-select -s /Applications/Xcode_16.2.app/Contents/Developer
  #     - name: Install Fastlane
  #       run: brew install fastlane
  #     - name: Run Unit Tests
  #       run: fastlane unit_test
  #     - name: Run UI Tests
  #       run: fastlane ui_test
  #     - name: Upload test results
  #       uses: actions/upload-artifact@v4
  #       if: always()
  #       with:
  #         name: test-results
  #         path: fastlane/test_output/
