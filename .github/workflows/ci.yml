name: CI

on:
  push:
    branches: [develop, main, master]
  pull_request:
    branches: [develop, main, master]

jobs:
  # 防止直接推送到保护分支
  prevent-direct-push:
    name: Prevent Direct Push
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && (github.ref == 'refs/heads/develop' || github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
    steps:
      - name: Block direct push to protected branch
        run: |
          echo "❌ 直接推送到保护分支被禁止!"
          echo "请使用Pull Request进行代码合并"
          echo "推送的分支: ${{ github.ref }}"
          echo "触发事件: ${{ github.event_name }}"
          exit 1

  swiftlint:
    name: SwiftLint
    runs-on: macos-15

    steps:
      - uses: actions/checkout@v4

      - name: Install SwiftLint
        run: |
          echo "Installing SwiftLint via Homebrew..."
          if ! command -v brew &> /dev/null; then
            echo "Homebrew not found, installing..."
            /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
          fi
          brew install swiftlint

      - name: Run SwiftLint
        run: |
          swiftlint --reporter github-actions-logging --strict

  # build:
  #   name: Build
  #   runs-on: macos-15

  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Setup Xcode
  #       run: |
  #         sudo xcode-select -s /Applications/Xcode_16.2.app/Contents/Developer
  #         xcodebuild -version

  #     - name: Install iOS Platform
  #       run: |
  #         echo "=== 📱 安装 iOS 平台 ==="

  #         # 显示当前已安装的平台
  #         echo "📋 当前已安装的平台:"
  #         xcodebuild -showsdks

  #         # 显示可用的运行时
  #         echo "📋 当前可用的运行时:"
  #         xcrun simctl list runtimes

  #         # 尝试安装最新的 iOS 模拟器运行时
  #         echo "📥 尝试安装 iOS 模拟器运行时..."

  #         # 方法1: 使用 xcodebuild 安装 iOS 平台
  #         echo "📥 尝试下载 iOS 平台..."
  #         xcodebuild -downloadPlatform iOS || echo "方法1失败，继续尝试其他方法"

  #         # 方法2: 使用 Xcode 直接安装组件
  #         echo "📥 尝试安装 iOS 18.2 运行时..."
  #         sudo xcode-select --install 2>/dev/null || echo "command line tools 已安装"

  #         # 方法3: 检查并安装缺失的模拟器
  #         echo "📥 检查模拟器安装状态..."
  #         xcrun simctl list runtimes | grep iOS || echo "需要安装 iOS 运行时"

  #         # 尝试使用较老但稳定的 iOS 版本
  #         echo "🔄 如果 18.2 不可用，将使用可用的最新版本"

  #         # 显示安装后的状态
  #         echo "📋 安装后的平台状态:"
  #         xcodebuild -showsdks | grep simulator

  #         echo "📋 安装后的运行时:"
  #         xcrun simctl list runtimes ios

  #     - name: Simple Build
  #       run: |
  #         echo "=== 🚀 简单直接的 iOS 构建 ==="

  #         # 显示可用设备（调试用）
  #         echo "📱 可用的模拟器设备："
  #         xcrun simctl list devices | grep "iPhone" | head -5

  #         # 显示可用的 SDK
  #         echo "📱 可用的 iOS SDK："
  #         xcodebuild -showsdks | grep simulator

  #         # 智能选择可用的目标
  #         echo "🎯 智能选择构建目标..."

  #         # 尝试多个设备选项（按优先级）
  #         DEVICES=("iPhone 16" "iPhone 15" "iPhone 14" "iPhone 13")
  #         SELECTED_DEVICE=""

  #         for device in "${DEVICES[@]}"; do
  #           if xcrun simctl list devices | grep -q "$device"; then
  #             SELECTED_DEVICE="$device"
  #             echo "✅ 选择设备: $SELECTED_DEVICE"
  #             break
  #           fi
  #         done

  #         if [ -z "$SELECTED_DEVICE" ]; then
  #           echo "❌ 未找到合适的设备，使用默认"
  #           SELECTED_DEVICE="iPhone 15"
  #         fi

  #         # 使用最简单的参数
  #         xcodebuild \
  #           -project ZHKLine.xcodeproj \
  #           -scheme ZHKLine \
  #           -destination "platform=iOS Simulator,name=$SELECTED_DEVICE,OS=latest" \
  #           -configuration Debug \
  #           CODE_SIGNING_REQUIRED=NO \
  #           CODE_SIGN_IDENTITY="" \
  #           PROVISIONING_PROFILE="" \
  #           DEVELOPMENT_TEAM="" \
  #           build

  #         echo "✅ 构建成功完成！"

  # test:
  #   name: Test
  #   runs-on: macos-15
  #   needs: build
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Select Xcode version
  #       run: sudo xcode-select -s /Applications/Xcode_16.2.app/Contents/Developer
  #     - name: Install Fastlane
  #       run: brew install fastlane
  #     - name: Run Unit Tests
  #       run: fastlane unit_test
  #     - name: Run UI Tests
  #       run: fastlane ui_test
  #     - name: Upload test results
  #       uses: actions/upload-artifact@v4
  #       if: always()
  #       with:
  #         name: test-results
  #         path: fastlane/test_output/
